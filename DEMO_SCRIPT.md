# 시연 스크립트 (발표 2~3분 + 라이브 데모)

## 0. 사전 준비 (발표 시작 전)
- [ ] `npm run dev` 실행, `localhost:3000/dashboard.html` 탭 미리 열어두기
- [ ] `.env`에 실제 Gmail 앱 비밀번호 설정 완료 확인 (발송 데모를 진짜로 할 경우)
- [ ] 슬라이드 탭도 미리 열어두기: https://claude.ai/artifact/WMR1vNSYtk4u4Gd1eh3LCt
- [ ] 본인 Gmail 받은편지함 탭도 하나 띄워두기

---

## 1. 슬라이드 발표 (2~3분)

### 1p. 문제/해결책 (30초)
> "경영 임원분들은 매일 아침 판매, 생산, CAPA, 재고 같은 KPI를 4개나 직접 들어가서 확인하고 비교해야 합니다. 시간은 제한적인데, 어떤 걸 먼저 봐야 할지 판단하는 데만 시간이 걸리고, 이상 징후를 놓치기도 쉽습니다.
> 그래서 저는 AI가 매일 아침 이 KPI들을 먼저 분석해서, 오늘 확인이 필요한 이슈만 딱 골라 이메일로 보내주는 서비스를 만들었습니다."

### 2p. AI Agent Workflow (40초)
> "동작 방식은 세 단계입니다. 첫째, 매일 KPI 데이터를 자동으로 분석해서 정해진 기준(±5%p, 90%, 5%)으로 이상 여부를 판정합니다. 둘째, 샘플 데이터 1,200건 이상을 만들 때 Haiku 모델 서브에이전트를 병렬로 돌려서, 메인 작업을 멈추지 않고 동시에 진행했습니다. 셋째, 계산 결과를 미리 정해둔 값과 대조 검증하는 과정에서 실제 데이터 오류를 2건 찾아서 고쳤습니다."

### 3p. 이렇게 적용했습니다 (40초)
> "이번 교육에서 배운 걸 그대로 썼습니다. 정해진 시간에 자동 실행되는 방법은 매일 아침 8시 이메일 자동발송에, 데이터랑 로직을 나눠서 관리하는 방법은 KPI 기준이 바뀌어도 숫자 파일만 바꾸면 되게 만드는 데, 작업 기록을 남기는 방법은 오늘 개발 전체 과정을 추적하는 데 썼습니다."

### 4p. 완성도 & 현업 적용 (30초)
> "화면 3개 다 실제로 동작하고, 브라우저에서 직접 테스트도 끝냈습니다. Gmail 계정만 연결하면 바로 운영으로 전환할 수 있고, 다른 팀도 데이터 파일만 바꾸면 그대로 쓸 수 있는 구조입니다."

---

## 2. 라이브 데모 (2~3분)

### Step 1 — 이메일 확인
1. Gmail 받은편지함 탭으로 이동
2. (사전에 `npm run send:now`로 미리 보내둔) "Biz Daily Briefing" 메일 열기
3. 멘트: "오늘 아침 8시에 이 메일이 자동으로 왔다고 가정해보겠습니다. 갤럭시Z폴드8, 아이폰18 듀오 판매 부진, Watch 생산 지연, A4 라인 가동률 저하, 부진재고 8개 제품 — 이렇게 4가지 이슈가 바로 눈에 들어옵니다."
4. "Biz Insight 바로가기" 버튼 클릭 (또는 클릭 시늉 후 대시보드 탭으로 전환)

### Step 2 — 경영정보 대시보드
1. `localhost:3000/dashboard.html` 탭으로 전환
2. 판매진척률 카드 가리키며: "카드 안에 원 세 개는 최근 3일간 일별 목표 달성률이고, 아래 박스가 실제 판정 기준인 9월 누적 진척률입니다."
3. 부진재고현황 카드 가리키며: "하단에 제품군별 부진재고 비율까지 한눈에 보입니다."
4. CAPA 가동률 카드: "라인별로 봤을 때 A4만 기준 미달인 걸 바로 알 수 있습니다."

### Step 3 — 메일링 관리
1. 상단 "메일링 관리" 탭 클릭
2. "미리보기" 버튼 클릭 → 오늘자 이메일이 새 탭에 그대로 뜨는 것 보여주기
3. "발송 스케줄" 탭 클릭 → 매일 08:00 자동 발송 설정 화면 보여주기
4. "수신처 관리" 탭 클릭 → 수신자 목록 보여주기

### Step 4 — 실제 발송 (선택, .env 설정된 경우만)
1. "발송하기" 탭으로 돌아가서 "지금 발송하기 ▶" 버튼 클릭
2. 발송 완료 메시지 확인
3. Gmail 탭으로 전환해서 새로고침 → 방금 온 메일 확인
4. 멘트: "이렇게 버튼 한 번으로 실제 발송까지 확인됩니다. 이제 Cron만 걸면 매일 아침 자동으로 똑같이 동작합니다."

---

## 3. 마무리 멘트
> "오늘 배운 자동화, 데이터-로직 분리, 작업 추적 방식을 그대로 살려서 하루 만에 실제로 동작하는 서비스를 만들었습니다. Gmail 연동만 마치면 바로 다음 주부터 실제 업무에 쓸 수 있습니다."
